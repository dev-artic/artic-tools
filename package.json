{
  "name": "artic-ptr-paytable",
  "version": "1.0.0",
  "description": "Artic PTR Paytable Dashboard",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "test:rules": "npx --yes firebase-tools@latest emulators:exec --only firestore,storage --project artic-ptr-paytable \"node --test tests/rules.test.mjs\""
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.15.0"
  }
}
